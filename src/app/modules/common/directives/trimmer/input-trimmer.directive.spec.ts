import { InputTrimmerDirective } from './input-trimmer.directive';

describe('InputTrimmerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTrimmerDirective();
    expect(directive).toBeTruthy();
  });
});
